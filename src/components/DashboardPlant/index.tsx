import { Line } from '../Modal/styles'
import { ContainerCard, FirstLine, Title } from './styles'

export const DashboardPlant = () => {
  return (
    <ContainerCard>
      <FirstLine>
        <Title>Plantas</Title>
        <Line />
      </FirstLine>
    </ContainerCard>
  )
}
